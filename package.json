{
  "name": "digitalcore-monorepo",
  "private": true,
  "scripts": {
    "build": "cd my-website && npm install --legacy-peer-deps && npm run build",
    "deploy": "npx vercel --prod"
  },
  "dependencies": {
    "express": "^4.18.2",
    "cors": "^2.8.5",
    "helmet": "^7.1.0",
    "morgan": "^1.10.0",
    "express-rate-limit": "^7.1.5",
    "dotenv": "^16.4.7",
    "serverless-http": "^3.2.0"
  }
}

